name: Test check_retries ignores original failure
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          report_paths: '${{ github.workspace }}/tests/fixtures/junit-reports/TEST-retry.xml'
          annotate_only: 'true'
          fail_on_failure: 'true'
          check_retries: 'true'
          job_summary: 'false'
          check_name: 'Retry Test Report'

      - name: Assert action succeeded (retry ignored original failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "success" ]; then
            echo "FAIL: expected action to succeed when check_retries=true and test passed on retry, got '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly succeeded with check_retries"

      - name: Assert total count
        env:
          TOTAL: ${{ steps.action.outputs.total }}
        run: |
          echo "total: $TOTAL"
          if [ -z "$TOTAL" ]; then
            echo "FAIL: expected non-empty total"
            exit 1
          fi
          echo "PASS: total=$TOTAL"
